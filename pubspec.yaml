name: smartpredi
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.16.2 <3.0.0"

dependencies:
  camera: ^0.10.0+4

  flutter:
    sdk: flutter
  fluttertoast: ^8.0.9
  tflite: ^1.1.2
  tflite_flutter: ^0.8.0
  image_picker: ^0.8.5+3
  provider: ^6.0.3
  google_fonts: ^3.0.1


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
  assets:
      - assets/model_electro.tflite
      - assets/labels.txt
      - assets/care.PNG


